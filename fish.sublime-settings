{
  // Only for nonstandard fish installs: Absolute file path to the directory containing the fish_indent binary
  "fish_indent_directory": "",

  // Indent the current fish file immediately prior to each save
  "indent_on_save": false,

  // Only if indent_on_save is true: List of files not to indent when saving
  "blacklist": ["syntax_test_fish.fish"]
}
